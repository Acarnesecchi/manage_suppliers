{% extends 'base.html.twig' %}

{% block title %}Add a new supplier{% endblock %}

{% block body %}
    <h1>Add a new supplier</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-back">Save</button>
    {{ form_end(form) }}
{% endblock %}