{% extends 'base.html.twig' %}

{% block title %}Edit Supplier{% endblock %}

{% block body %}
<h1>Edit Supplier</h1>

<div class="form-wrapper">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-save">Save changes</button>
{{ form_end(form) }}
</div>

<a href="{{ path('app_supplier_delete', {'id': supplier.id}) }}" class="btn btn-delete" onclick="return confirm('You are about to delete a resource. This action is irreversible')">Delete</a>
<button class="btn btn-back" onclick="window.location.href='{{ path('app_supplier_index') }}'">Back to list</button>
{% endblock %}